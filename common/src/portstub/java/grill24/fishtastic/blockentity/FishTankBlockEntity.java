package grill24.fishtastic.blockentity;

/**
 * Portstub (gradle/port-excludes.gradle): temporary stand-in for the real 1111-line
 * {@code FishTankBlockEntity} (extends {@code BlockEntity}, implements {@code Container},
 * {@code MenuProvider}), which pulls in the full tank/menu/item registration graph —
 * B2.6/B2.7 territory, not B2.7's registration-cleanup slice. {@link
 * grill24.fishtastic.architectury.IRegistrationApi#createFishTankBlockEntity} only needs the
 * type name to resolve as a return type here. Real file still exists (excluded) at
 * blockentity/FishTankBlockEntity.java; delete this stub once that graph is ported for real.
 */
public class FishTankBlockEntity {
}
