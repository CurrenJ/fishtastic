{
  "name": "fishtastic-mcp-bridge",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "Thin MCP stdio shim that forwards tool calls to the fishtastic mod's embedded local HTTP bridge (see /fishtastic mcp start in-game).",
  "engines": {
    "node": ">=18"
  },
  "main": "index.js",
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.12.0",
    "zod": "^3.23.8"
  }
}
